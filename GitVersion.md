## 0.3x

### 0.30

重新开始这个搭建工程，测试开机自检

#### 0.30.1

新增对于该UI系统的开机自动启动的停止设置脚本

[stop_hardware_ui_autostart.sh](D:\\STM32Project\\202607RaspiUI\\UICode\\UI\\stop_hardware_ui_autostart.sh)

#### 0.30.2

继续检查了ADS的问题，发现接线松了，已经修复好

### 0.31

在该版本中，要完成以下构建任务（不只是本系统的，还有大物报告的）：

1. 光电效应产生光功率（跨阻放大的电压、样品的电阻、跨阻前电流）；光电效应LED灯的功率；这样就可以计算光电转换的效率

2.制作说明视频时，左上方VNC或者Nomachine实时屏幕动向，右上角可以添加摄像头情况

3.按键重新校准

4.改写触控IPS Pygame UI

#### 0.31.1

按键按照现在的底板设计， 进行了重新映射

#### 0.31.2

现在发现了ADS1256自检过于严苛，其实有了转接板之后，硬件连接出错比连接还难，相信XH2.54；修改没有问题

#### 0.31.3

现在更换了IPS触摸1024*600屏幕，所以UI要改写，本来以键盘为交互核心的UI逻辑要大改，改为以触控为核心的UI交互逻辑；键盘什么时候有用，只有在数字键盘交互的时候有用，而不是控制UI的核心了，数字键盘现在只有两个地方，第一个就是转盘的角度输入，还有一个就是PWM调光的输入；修改，大改UI

#### 0.31.4

UI现在需要改进，就是：

1. FFT屏蔽得了，反正用不上这个功能，但是相关函数暂时不删除，只是UI主程序不调用了
2. 底下的一排按钮先全部去除了，主要是侧边的灯控制、PWM、Camera太小了
3. 对于LED的触控控制，按照原来的设计，以LED的选择位置（还是左右箭头选择，中间为当前选中的展示，触屏点击中间的展示，进入到子页面（还是只有左边小页）就是对于角度的控制，在触控子页面就是分为 +- 0.1 ° ，+-0.5° +- 5° 三种，然后底下是可以选择手动矩阵输入，点一下就跳出矩阵键盘的输入，可以是触控输入也可以是矩阵键盘输入，限制是0~369.99°）
4. 对于PWM的控制，也是这样，就是中间是展示主PWM大小，然后有一个按钮，就是对于步进的循环选择，分为0.1% 1% 5%，然后左右的按钮，就是左右角度的调整；点击中间的当前亮度展示，可以调出键盘进行细致调整，精准到小数点后两位
5. 对于摄像机的控制，相同的样式，就是中间展示现在是什么模式，左右选择样式切换
6. 对于 暂停测量、清空曲线，这两个按钮，放在 “`光电流激发与测量系统`”（改一下标题）的右边，高度和标题栏等高
7. 对于UI的风格，现在是扁平化设计，但是我需要你修改到 类似Windows XP Luna 蓝色主题这种嵌入式、工控设备的复古UI，而不是现在的扁平化
8. 光电流实时曲线的标题改为“光电流曲线”，然后做一个双指控制的驱动，就是左右双指可以缩放时间轴，上下双指缩放电压轴
9. 以上设计的思路，均不用再UI里面说明，只要实现即可，不能出现“类Windows XP Luna 蓝色主题 ”这种刻意强调的傻逼说明

#### 0.31.5

UI继续改进：

1. 光电流标题底下的实时系统信息，放到摄像画面的下方，单独做一个高度不高的块，然后如果一行放不下（超宽），那就滚动播放，这个块不需要标题
2. 开机自检的界面，字有些看不清，改为使用灰色字改为白色字，进度条也显眼一些![1785577766048](image/GitVersion/1785577766048.png)
3. 对于左侧栏，有些挡住会看不清，比如灯位转盘的“已到位”，把这种放在这个块标题的左边，就是表示这个功能当前的状态，可以是[已到位]这样表示
4. 左侧栏，很多字偏小，很小家子气，比如400nm，大一些，然后这个灯位转盘的中间按钮块，显示的时候中间有一个间隔线，就是上是400nm（字大些，要70%左右填满的大小），下是当前角度；左右的两个按钮，上部画>或<，下部就是下一个灯的预告，字大些；同理设计另外的PWM、摄像块
5. 对于曲线展示，曲线双轴缩放似乎无效也较难实现，去除；再这个曲线的展示模块的下方，做一个一排的按钮组，可以实现时间轴/电压轴的缩放（共四个），然后采样点、已滤尖峰、测量时间、再加一个v/div ms/div显示，放在曲线的上部，每一个各做一个小块，而不是现在文字散着，小块可以小一些

![1785578362996](image/GitVersion/1785578362996.png)

1. 顶栏的两个按钮，MEASURE、600点这种辅助说明，去除吧

#### 0.31.6

UI继续调整：

1. 左侧栏字太大了，450nm等字，比标题小一些，最多到标题的大小； 步进 字小一些，有点超出模块大小了
2. 消息栏，我的设计要求是在摄像画面底下，而不是在光电流曲线模块底下
3. 光电流块，底下的时间轴的左右时间限位显示，和时间-+、电压-+的块冲突，注意这四个块高度低一些，不要冲突；然后电压-+ 文字改为 电压轴 -+ ,时间 改为 时间轴

#### 0.31.7

左侧栏，整体可以压缩一些，现在有点空，就是按钮的留白较多，就是现在左侧栏的字大小，不要再动了，但是左侧栏的各个宽度，其实可以略窄，以此缩小整个左侧栏；

另外，步进：1% 这个左侧栏的小块按钮，不要换行，直接显示，比如显示为 步进: 0.1%这种

#### 0.31.8

发现了ADS1256 的驱动问题，对照数据手册继续修正了

#### 0.31.9/10/11/12/13/14

继续修改ADS驱动问题

#### 0.31.15

 添加新的STM32-ADS1256中间件的测试，测试一次成功，非常好！！！

#### 0.31.16

现在要根据UICode\ADS1256\stm32_parallel_test.py这个测试，设计一个ADS1256-STM32的新驱动，使得我的树莓派UI可以使用此进行电压曲线的绘制

#### 0.31.17/18/19

D:\STM32Project\202607RaspiUI\UICode\UI\run_hardware_ui.sh 这个改写为就是树莓派UI服务，开机自动启动的配置和开启脚本

`bash UI/run_hardware_ui.sh enable`

关闭开机自启：

```
bash UI/run_hardware_ui.sh disable
```

#### 0.31.20

整理代码，将原来的ADS树莓派直接驱动放到UICode\ADS1256\Old_GPIO_Driver 里面去

#### 0.31.21

现在，系统将使用CSI摄像头，增加了一个测试的脚本（因为USB摄像头，打电赛捐了），脚本在UICode\CSICamera

#### 0.31.22

其他没有什么问题，就是我拍黄色的人脸，怎么是蓝色的啊，蓝色的鼠标垫拍成黄色，是不是色彩的驱动处理有问题

#### 0.31.23

的确是色彩线序做错了，应该使用 `python3 CSICamera/test_csi_camera_display0.py --display :0 --channel-order bgr` 这个就是没问题了；然后现在就是做一个CSI驱动适配于UI主程序，现在的UI程序放弃MF500 USB 摄像机的驱动，改为CSI的驱动

#### 0.31.24/25

添加一个校准灯的机制，首先就是将400nm的灯设为校准灯，然后用户开机会校准这个灯的位置，直到这个灯校准合适，这个校准值要记录在一个Json文件里面永久化；对于其他的灯，在这个校准值的基准上，进行每隔60°的调整，然后用户会进行其他灯的微调，其他灯的相对微调角度也是要记录下来，这样用户每次开机就可以得到得到很舒服的灯位置；摄像画面块下方的系统信息，位置和摄像画面的情况解耦，现在的情况是开启设想头，这个块会跟着移动到摄像画面的下方，我的设计要求是这个系统信息块一直在UI的左下角，不随这个摄像而变化
